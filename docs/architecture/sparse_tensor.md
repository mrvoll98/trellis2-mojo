# Architecture: SparseTensor

Central to efficiency: operates only on active voxels instead of dense 3D.

Used in SLAT flows, VAEs (sparse unet), samplers.

See mapping/sparse/basic.md and conversion/sparse_tensor_in_mojo.md
