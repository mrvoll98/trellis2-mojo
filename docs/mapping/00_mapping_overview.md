# Mapping Overview

Every major file/component mapped to proposed Mojo equivalent.

See sub pages.

**Key insight:** SparseTensor is the central data structure. Port it early.

Start port with reusable utils + small pure math (rope, flow_euler).
